#!/bin/bash

# Get the name of the Python script from the environment variable $PYFILE
PYFILE=$1

# Check if the Python script exists
if [ ! -f "$PYFILE" ]; then
  echo "Python script '$PYFILE' does not exist."
  exit 1
fi

# Run the Python script
python "$PYFILE"

